// [file: jvm.stg]
// Template for Example codegenerator emitting JVM ByteCode.

group tmg;

jvmFile(instructions) ::= <<
; JVM assembler code generated by CalcCodeGenerator
<instructions; separator="\n">
HALT                                    ; end of program
>>

declare(var, addr) ::= <<
>>

assign(var, addr, expr) ::= <<
>>

print(expr) ::= <<
>>

binexpr(x, y, instr) ::= <<
>>

loadvar(var, addr) ::= <<
>>

loadnum(val) ::= <<
>>